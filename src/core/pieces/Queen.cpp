#include "Queen.hpp"

Queen::Queen(int x, int y, Color color)
    : Piece(x, y, color)
{
}

std::vector<std::pair<int, int>> Queen::getPossibleMoves() const {
    std::vector<std::pair<int, int>> moves;

    return moves;
}