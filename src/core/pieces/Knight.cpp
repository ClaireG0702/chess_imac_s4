#include "Knight.hpp"

Knight::Knight(int x, int y, Color color)
    : Piece(x, y, color)
{
}

std::vector<std::pair<int, int>> Knight::getPossibleMoves() const {
    std::vector<std::pair<int, int>> moves;

    return moves;
}